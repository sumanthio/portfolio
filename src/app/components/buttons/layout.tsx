import React from 'react';

export const metadata = {
  title: 'Buttons | Component Library',
  description: 'Various button styles, sizes, and states used in the design system.',
};

export default function ButtonsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 