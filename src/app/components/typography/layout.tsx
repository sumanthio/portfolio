import React from 'react';

export const metadata = {
  title: 'Typography | Component Library',
  description: 'Typography styles and text formatting used in the design system.',
};

export default function TypographyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 