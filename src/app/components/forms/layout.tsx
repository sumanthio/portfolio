import React from 'react';

export const metadata = {
  title: 'Forms | Component Library',
  description: 'Form components, inputs, and interactive elements used in the design system.',
};

export default function FormsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 